const dbConfig = {
  host: "localhost",
  port:8081,
  user: "root",
  password: "",
  database: "stl",
  waitForConnections: true,
  connectionLimit: 10,
  queueLimit: 0,
};

export default dbConfig;
